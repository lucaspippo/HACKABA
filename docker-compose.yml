services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: polpilot
      POSTGRES_PASSWORD: polpilot
      POSTGRES_DB: polpilot
    ports:
      - "5432:5432"
    volumes:
      - polpilot_db_data:/var/lib/postgresql/data

volumes:
  polpilot_db_data:
